# Hand Beat 仕様書

## 0. 文書情報

- プロジェクト名: **Hand Beat**
- 種別: Webブラウザ向けカメラ式リズムゲーム
- 主要技術: Next.js / TypeScript / MediaPipe Hand Landmarker / Web Audio API / Supabase / Vercel
- ソース管理: GitHub
- 配布: GitHub と Vercel を連携し、main ブランチ更新時に本番デプロイ
- 本仕様書の目的: Codex が本書と `資料/` 配下の素材を読み、MVPから本番公開可能な状態まで一貫して実装できるようにする

---

# 1. プロジェクト概要

Hand Beat は、Webカメラに映るプレイヤー自身の映像をゲーム画面として使用し、画面内に表示されるノーツに合わせて手を動かす体感型リズムゲームである。

従来の音ゲーのようにキーボードやコントローラーを押すのではなく、MediaPipe により手の形と両手の動きを認識し、指定されたジェスチャーを指定位置・指定タイミングで成立させることでノーツを処理する。

ゲーム画面の大部分はカメラ映像とし、その上にノーツ、判定サークル、スコア、コンボ、楽曲情報などをオーバーレイ表示する。

プレイヤーが「自分自身の身体を使って音楽を演奏している感覚」を持てることを最優先する。

---

# 2. プロダクトコンセプト

## 2.1 コンセプト

**その手で、音を刻め。**

画面に現れるノーツへ手を合わせ、指定された手の形をタイミングよく作ることで音楽を演奏する。

## 2.2 目的

1. 手を使って直感的に遊べる音ゲーを実現する
2. MediaPipe を技術デモとして見せるのではなく、ゲーム体験として自然に利用する
3. 初めてプレイする人でも数十秒でルールを理解できる
4. 音楽・譜面・画像を後から簡単に追加できる
5. Webブラウザだけでプレイできる
6. グローバルランキングによってリプレイ性を持たせる
7. GitHubへのPushからVercelへ自動デプロイできる構成とする

---

# 3. デザイン方針

## 3.1 世界観

以下の方向性とする。

- 近未来
- ネオン
- 音楽ゲーム
- サイバー感
- ダーク背景
- 発光するUI
- カメラ映像を邪魔しない半透明UI
- ゲームセンターの音ゲーをWebに持ち込んだような雰囲気

ただし、以下は避ける。

- 「生成AIが作ったUI」に見える過剰な装飾
- 不自然なグラデーションの多用
- 読めない小さな文字
- 意味のない光や粒子
- 3D要素の過剰使用
- 一画面に情報を詰め込みすぎること
- AI、ロボット、脳、回路などの直接的なAI記号
- 画像内に意味不明な文字を生成させること

UIは「AI風」ではなく、**完成された商用音楽ゲーム風**を目指す。

## 3.2 UI画像生成の利用

以下は画像生成を活用してよい。

- タイトルロゴ
- 楽曲ジャケット
- ノーツアイコン
- ボタン装飾
- 背景パターン
- RESULT画面の装飾
- ランキング用アイコン
- 難易度バッジ

ただし、ボタンの文字そのものは画像に焼き込まず、原則 HTML/CSS のテキストとして重ねる。

理由:
- 可読性
- 多言語化
- レスポンシブ対応
- アクセシビリティ
- 画像生成由来の文字崩れ防止

## 3.3 UIカラー

ノーツは色だけで識別させない。
必ず **色 + アイコン +形** で識別できるようにする。

推奨:

- FIST: 赤系
- GUN: 青系
- OPEN: 緑系
- CLAP: オレンジ系

---

# 4. 入力ジェスチャー

MVPでは以下の4種類を利用する。

| ID | 表示 | ジェスチャー | 基本音 |
|---|---|---|---|
| `fist` | ✊ FIST | 片手をグー | Kick |
| `gun` | 👉 GUN | 親指+人差し指を伸ばした指鉄砲 | Snare |
| `open` | ✋ OPEN | 片手をパー | Hi-Hat |
| `clap` | 👏 CLAP | 両手を合わせる拍手 | Clap |

## 4.1 FIST

判定条件の概念:

- 人差し指: 曲がっている
- 中指: 曲がっている
- 薬指: 曲がっている
- 小指: 曲がっている
- 親指の状態は多少許容する

## 4.2 GUN

判定条件の概念:

- 人差し指: 伸びている
- 親指: 伸びている
- 中指: 曲がっている
- 薬指: 曲がっている
- 小指: 曲がっている

## 4.3 OPEN

判定条件の概念:

- 人差し指: 伸びている
- 中指: 伸びている
- 薬指: 伸びている
- 小指: 伸びている

## 4.4 CLAP

CLAPは静止姿勢ではなくイベントとして判定する。

条件:

1. 左右両手が検出されている
2. 一定時間前は両手が離れている
3. 両手の手のひら中心間距離が急速に縮まる
4. 距離がしきい値以下に入る
5. 上記条件を満たした瞬間に `CLAP` イベントを1回発火
6. 発火後は 150〜250ms 程度のクールダウンを設ける
7. 両手を合わせたままでは再発火しない
8. 再度一定距離以上離れてから次のCLAPを許可する

---

# 5. MediaPipe認識仕様

## 5.1 基本方針

MediaPipe Hand Landmarker を利用し、手の21ランドマークからジェスチャーを分類する。

最大2手まで追跡する。

認識処理とゲーム判定処理は完全に分離すること。

例:

```text
Camera
  ↓
MediaPipe Hand Landmarker
  ↓
Landmarks
  ↓
Gesture Classifier
  ↓
Gesture Event
  ↓
Game Engine
```

## 5.2 認識ブレ対策

1フレームだけ別ジェスチャーになった場合は即確定しない。

推奨:

- 通常ジェスチャー: 2〜3フレーム安定で確定
- CLAP: 動きイベントのため別ロジック

ただし音ゲーの遅延につながるため、安定フレーム数は定数化する。

```ts
GESTURE_STABLE_FRAMES
CLAP_DISTANCE_THRESHOLD
CLAP_COOLDOWN_MS
```

を設定ファイルから変更できるようにする。

## 5.3 ジェスチャー入力

ゲームへの入力イベントは以下の形式を基本とする。

```ts
type GestureType = "fist" | "gun" | "open" | "clap";

interface GestureEvent {
  gesture: GestureType;
  timestampMs: number;
  hand?: "left" | "right" | "both";
  confidence: number;
}
```

通常ジェスチャーは、前回確定ジェスチャーから別ジェスチャーに変化した瞬間を入力として扱う。

同じジェスチャーを出し続けても連続入力扱いにしない。

---

# 6. ゲームプレイ仕様

## 6.1 最大の特徴

**画面全体がカメラ映像である。**

カメラ映像の上にノーツを表示する。

通常の縦スクロール音ゲーのようにノーツがレーンを落下する方式ではなく、プレイヤーの身体周辺の複数位置へノーツが表示される。

ノーツ位置へ手を動かしながら、指定ジェスチャーを成立させる。

## 6.2 ノーツ表示

各ノーツは以下の要素を持つ。

- ノーツ本体
- ジェスチャーアイコン
- ノーツ色
- 外周判定サークル
- 出現アニメーション
- 成功/失敗エフェクト

ノーツ出現時、ノーツ本体の外側に大きな円を表示する。

時間経過に伴って円が縮小し、ノーツ本体の外周と一致した瞬間が基準判定時刻となる。

```text
出現直後

   ┌───────────────┐
   │   大きな円      │
   │       ✊        │
   └───────────────┘

       ↓ 縮小

       ◯
      ✊

       ↓

      ◎  ← PERFECTタイミング
      ✊
```

プレイヤーはサークルが一致するタイミングで、ノーツ位置へ手を移動して指定ジェスチャーを行う。

## 6.3 位置判定

ジェスチャーが正しいだけでは成功にしない。

以下を両方満たす必要がある。

1. 正しいジェスチャー
2. 指定ノーツ位置の判定領域内に手が存在する

判定座標はカメラ映像上の正規化座標を使用する。

```ts
interface NotePosition {
  x: number; // 0.0 - 1.0
  y: number; // 0.0 - 1.0
}
```

例:

```json
{
  "x": 0.25,
  "y": 0.45
}
```

画面解像度に依存しない。

## 6.4 手の位置

通常ノーツ:

- 手のひら中心座標を基本判定座標とする

CLAP:

- 左右の手のひら中心の中点を判定座標とする

## 6.5 判定

初期値:

| 判定 | 時間差 |
|---|---:|
| PERFECT | ±80ms |
| GREAT | ±150ms |
| GOOD | ±250ms |
| MISS | ±250ms超 |

位置判定にも許容半径を持たせる。

難易度ごとに位置許容範囲を変えてもよい。

例:

- EASY: 広い
- NORMAL: 標準
- HARD: やや狭い

ただし初期MVPではプレイヤー体験を優先し、厳しすぎない設定にする。

---

# 7. 楽曲・譜面の最重要仕様

## 7.1 絶対ルール

**同じ楽曲・同じ難易度では、全プレイヤーでノーツのタイミング・順番・位置・種類を完全に統一する。**

ランダム生成は禁止。

ゲーム開始時にAIが譜面を生成することも禁止。

同じ条件なら、何回プレイしても100%同じ譜面を使用する。

理由:

- スコアランキングを公平にする
- プレイヤーが譜面を練習・記憶できる
- リプレイ性を持たせる
- スコア検証を可能にする
- バグ再現性を確保する

## 7.2 一意キー

譜面は以下の組み合わせで一意にする。

```text
songId + difficulty + chartVersion
```

例:

```text
neon-pulse + hard + 1
```

## 7.3 難易度

初期は以下3段階。

```text
easy
normal
hard
```

将来:

```text
expert
```

を追加可能な設計にする。

## 7.4 難易度の設計思想

### EASY

- ノーツ数少なめ
- 余裕のあるタイミング
- 移動距離小さめ
- FIST / GUN / OPEN中心
- CLAP少なめ

### NORMAL

- 4種類すべて使用
- 適度な位置移動
- 8分音符相当まで
- サビで密度を上げる

### HARD

- 高密度
- 大きめの位置移動
- CLAPを効果的に使用
- 同一ジェスチャー連打は避けつつ高速切替
- 曲のキック・スネア・ハイハット・クラップとの対応を意識

---

# 8. 譜面ファイル

譜面はJSONで管理する。

例:

```json
{
  "songId": "neon-pulse",
  "difficulty": "normal",
  "chartVersion": 1,
  "level": 5,
  "durationMs": 182000,
  "offsetMs": 0,
  "notes": [
    {
      "id": "n0001",
      "timeMs": 1250,
      "gesture": "fist",
      "x": 0.25,
      "y": 0.55
    },
    {
      "id": "n0002",
      "timeMs": 1750,
      "gesture": "gun",
      "x": 0.72,
      "y": 0.42
    },
    {
      "id": "n0003",
      "timeMs": 2250,
      "gesture": "open",
      "x": 0.42,
      "y": 0.32
    },
    {
      "id": "n0004",
      "timeMs": 3000,
      "gesture": "clap",
      "x": 0.5,
      "y": 0.52
    }
  ]
}
```

## 8.1 `timeMs`

楽曲再生開始を `0ms` とした絶対時刻。

フレーム数では管理しない。

必ず楽曲の再生時刻を基準にする。

## 8.2 `offsetMs`

素材ごとの差を吸収するため曲単位で設定可能にする。

譜面ファイルを書き換えずに全ノーツを一括補正できるようにする。

## 8.3 譜面検証

ビルドまたはテスト時に以下を検証する。

- `songId` が存在する
- difficulty が許可値
- note id 重複なし
- timeMs が昇順
- timeMs が0以上
- timeMs が曲長を超えていない
- gesture が4種類のいずれか
- x / y が 0〜1
- chartVersion が整数
- 同じ songId + difficulty + chartVersion が重複しない

---

# 9. 音の対応

資料フォルダに以下の素材が存在する前提とする。

- 楽曲
- FIST用ヒット音
- GUN用ヒット音
- OPEN用ヒット音
- CLAP用ヒット音

例:

```text
FIST → kick
GUN  → snare
OPEN → hi-hat
CLAP → clap
```

プレイヤーが正しくノーツを叩いた瞬間、対応するヒット音を再生する。

楽曲に含まれるドラム音と完全に同じである必要はないが、違和感が出ないよう短いワンショットサウンドを利用する。

MISS時はヒット音を鳴らさない。

---

# 10. 音楽素材

楽曲は Gemini の楽曲生成を利用して制作した素材を使用する想定。

ゲーム実行時にGemini APIへ音楽生成を要求しない。

**完成済みの楽曲ファイルを静的アセットとして使用する。**

理由:

- 毎回同じ曲を保証する
- 譜面との同期を保証する
- APIコストを発生させない
- ロード時間を安定させる
- ランキング条件を統一する

---

# 11. 楽曲メタデータ

曲ごとに `metadata.json` を用意する。

例:

```json
{
  "id": "neon-pulse",
  "title": "Neon Pulse",
  "artist": "Hand Beat",
  "bpm": 128,
  "durationMs": 182000,
  "audio": "/assets/songs/neon-pulse/audio.mp3",
  "jacket": "/assets/songs/neon-pulse/jacket.webp",
  "previewStartMs": 32000,
  "difficulties": {
    "easy": {
      "level": 2,
      "chartVersion": 1
    },
    "normal": {
      "level": 5,
      "chartVersion": 1
    },
    "hard": {
      "level": 8,
      "chartVersion": 1
    }
  }
}
```

---

# 12. 曲追加フロー

新曲追加時にゲーム本体のロジックを変更してはならない。

基本フロー:

```text
楽曲生成
↓
音声ファイル配置
↓
ジャケット生成
↓
metadata.json作成
↓
easy.json作成
↓
normal.json作成
↓
hard.json作成
↓
譜面検証
↓
GitHub Push
↓
Vercel Preview確認
↓
mainへmerge
↓
Production Deploy
```

必要なのは原則データ追加のみ。

---

# 13. 画面構成

## 13.1 Title

表示:

- Hand Beat ロゴ
- キャッチコピー
- START
- HOW TO PLAY
- 音量設定
- カメラ利用についての短い表示

STARTでMain Menuへ移動。

## 13.2 Main Menu / Song Select

メインメニューと楽曲選択を一体化してよい。

表示:

- 曲ジャケット
- 曲名
- BPM
- 曲長
- 難易度
- 難易度レベル
- 自己ベスト
- 世界ランキング1位
- PLAY
- RANKING
- HOW TO PLAY

曲切替時に短いプレビューを再生してよい。

## 13.3 Ranking Preview

楽曲 × 難易度ごとの1位のみ表示する。

例:

```text
WORLD RECORD
1st   MIKI   98,720
```

クリックでランキング画面へ移動。

## 13.4 Camera Check

プレイ開始前に必ず表示。

確認項目:

- カメラ許可
- 手が検出できる
- FIST
- GUN
- OPEN
- CLAP
- カメラ左右反転
- 推奨距離

可能なら4ジェスチャーを1回ずつ成功させてからREADYを有効化する。

スキップ可能にしてもよい。

## 13.5 Game

最重要画面。

背景:
- カメラ映像をほぼ全画面表示
- プレイヤーから見て鏡のようになるよう水平反転

オーバーレイ:
- ノーツ
- 判定サークル
- PERFECT / GREAT / GOOD / MISS
- SCORE
- COMBO
- 曲名
- 難易度
- 曲進行
- 一時停止
- 必要最低限のHUD

プレイ中は説明パネルなど大きなUIを表示しない。

## 13.6 Result

表示:

- SCORE
- WORLD RANK
- PERSONAL BEST
- MAX COMBO
- PERFECT
- GREAT
- GOOD
- MISS
- ACCURACY
- NEW RECORD
- RETRY
- SONG SELECT
- RANKING

## 13.7 Ranking

曲 × 難易度ごとにランキングを表示。

MVP:
- TOP 100まで
- 1ユーザーにつきその譜面の最高スコア1件をランキング対象とする

---

# 14. スコア仕様

例:

```text
PERFECT = 1000
GREAT   = 700
GOOD    = 300
MISS    = 0
```

将来コンボボーナスを追加できる設計にするが、ランキング公平性のため計算式は `scoreVersion` を持たせる。

例:

```text
scoreVersion = 1
```

同一ランキング内で異なるscoreVersionを混在させない。

## 14.1 Accuracy

例:

```text
(PERFECT * 1.0 + GREAT * 0.7 + GOOD * 0.3) / totalNotes * 100
```

計算方式は共通関数に集約する。

---

# 15. スコア不正対策

ブラウザで動作するゲームなので、完全なチート防止は不可能である。

ただし、ランキングへ単純な偽スコアを送信できない構造にする。

## 15.1 禁止

ブラウザから以下のようなリクエストだけで登録してはならない。

```json
{
  "score": 99999999
}
```

## 15.2 プレイセッション

ゲーム開始時にサーバーから `playSessionId` を発行する。

セッションに紐づける:

- songId
- difficulty
- chartVersion
- scoreVersion
- startedAt
- nonce
- 有効期限

ゲーム終了時に以下を送信する。

```ts
interface ScoreSubmission {
  playSessionId: string;
  songId: string;
  difficulty: string;
  chartVersion: number;
  scoreVersion: number;
  events: {
    noteId: string;
    gesture: GestureType;
    judgedAtMs: number;
    timingOffsetMs: number;
    judgement: "perfect" | "great" | "good" | "miss";
  }[];
}
```

## 15.3 サーバー側再計算

Vercel側のServer Routeで:

1. sessionを検証
2. 正規の譜面を取得
3. noteIdを照合
4. note順序を確認
5. gestureを確認
6. timingOffsetを検証
7. 判定を再計算
8. scoreを再計算
9. 矛盾があれば拒否
10. 正常ならSupabaseへ保存

クライアント送信の最終score値は信用しない。

サーバーが算出したscoreのみDBへ保存する。

## 15.4 補足

位置判定はクライアント側MediaPipe情報なので完全なサーバー再現はできない。
MVPでは時間・ノーツ・種類・セッション整合性を主な不正対策とする。

---

# 16. Supabase

## 16.1 利用範囲

- Auth
- Ranking DB
- Profile
- Score / Play Result

MVPではログインなしのゲストプレイを許可してよい。

ランキング登録時のみ以下のどちらかを採用する。

推奨:
- Supabase Anonymous Auth を利用し、ユーザーIDを作成
- 表示名を初回ランキング登録時に設定

将来的に:
- Google Login
- GitHub Login

を追加可能にする。

## 16.2 テーブル案

### profiles

```sql
id uuid primary key
display_name text not null
created_at timestamptz not null
updated_at timestamptz not null
```

### play_sessions

```sql
id uuid primary key
user_id uuid
song_id text not null
difficulty text not null
chart_version int not null
score_version int not null
nonce text not null
started_at timestamptz not null
expires_at timestamptz not null
consumed_at timestamptz
```

### scores

```sql
id uuid primary key
user_id uuid not null
song_id text not null
difficulty text not null
chart_version int not null
score_version int not null
score int not null
max_combo int not null
perfect_count int not null
great_count int not null
good_count int not null
miss_count int not null
accuracy numeric not null
created_at timestamptz not null
```

推奨インデックス:

```text
song_id
difficulty
chart_version
score_version
score DESC
user_id
```

## 16.3 ランキング取得

ランキングキー:

```text
songId
difficulty
chartVersion
scoreVersion
```

同じユーザーが複数回プレイした場合、その条件で最高スコアだけランキング表示する。

---

# 17. Supabaseセキュリティ

RLSを必ず有効にする。

基本方針:

```text
profiles
SELECT: 公開範囲のみ
UPDATE: 本人のみ

scores
SELECT: ランキング用として許可
INSERT: ブラウザから直接禁止
UPDATE: 禁止
DELETE: 禁止

play_sessions
直接公開しない
```

スコア登録はVercelのサーバー処理を経由する。

SupabaseのSecret / Service Role相当のキーはブラウザへ公開しない。

`.env.local` または Vercel Environment Variables で管理する。

GitHubへ秘密情報をコミットしない。

---

# 18. Vercel / GitHub

## 18.1 Git運用

```text
main
  └─ production

feature/*
  └─ Pull Request
      └─ Vercel Preview
```

Pull RequestごとにPreview環境で確認する。

mainへmerge後、本番環境へデプロイ。

## 18.2 環境変数

例:

```text
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
SUPABASE_SECRET_KEY
```

秘密キーには `NEXT_PUBLIC_` を付けない。

## 18.3 GitHubに含めないもの

- `.env`
- `.env.local`
- Secret Key
- Service Role Key
- 管理用認証情報

---

# 19. 推奨プロジェクト構成

実際の素材の原本はユーザー指定通り `資料/` を維持する。

アプリ実行時に利用する素材は `public/assets/` に整理する。

```text
hand-beat/
│
├─ 仕様書.md
│
├─ 資料/
│  ├─ music/
│  │  └─ [作成済み楽曲]
│  ├─ sfx/
│  │  ├─ fist/
│  │  ├─ gun/
│  │  ├─ open/
│  │  └─ clap/
│  ├─ images/
│  │  ├─ ui-reference/
│  │  ├─ notes/
│  │  └─ jackets/
│  └─ README.md
│
├─ public/
│  └─ assets/
│     ├─ songs/
│     │  └─ neon-pulse/
│     │     ├─ audio.mp3
│     │     └─ jacket.webp
│     ├─ sfx/
│     │  ├─ fist.wav
│     │  ├─ gun.wav
│     │  ├─ open.wav
│     │  └─ clap.wav
│     └─ ui/
│
├─ src/
│  ├─ app/
│  │  ├─ page.tsx
│  │  ├─ menu/
│  │  ├─ play/[songId]/
│  │  ├─ result/
│  │  ├─ ranking/
│  │  └─ api/
│  │     ├─ play-session/
│  │     └─ scores/
│  │
│  ├─ components/
│  │  ├─ ui/
│  │  ├─ game/
│  │  └─ ranking/
│  │
│  ├─ game/
│  │  ├─ engine/
│  │  ├─ notes/
│  │  ├─ scoring/
│  │  ├─ timing/
│  │  └─ types/
│  │
│  ├─ mediapipe/
│  │  ├─ handLandmarker.ts
│  │  ├─ gestureClassifier.ts
│  │  ├─ clapDetector.ts
│  │  └─ gestureStabilizer.ts
│  │
│  ├─ data/
│  │  └─ songs/
│  │     └─ neon-pulse/
│  │        ├─ metadata.json
│  │        ├─ easy.json
│  │        ├─ normal.json
│  │        └─ hard.json
│  │
│  ├─ lib/
│  │  ├─ supabase/
│  │  ├─ validation/
│  │  └─ audio/
│  │
│  └─ styles/
│
├─ supabase/
│  ├─ migrations/
│  └─ tests/
│
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ e2e/
│
├─ scripts/
│  └─ validate-charts.ts
│
├─ .env.example
├─ .gitignore
├─ package.json
└─ README.md
```

---

# 20. `資料/` の取り扱い

Codexは作業開始時に最初に以下を確認する。

```text
仕様書.md
資料/
```

資料フォルダ内の音源・効果音・画像のファイル名と形式を確認し、実際に存在するファイルを使用する。

存在しない素材名を勝手に仮定しない。

原本は `資料/` に残す。

Web配信に必要なものを適切な名前へ整理・変換したうえで `public/assets/` へコピーして利用する。

音声変換が必要な場合、元ファイルを上書きしない。

---

# 21. 音声同期

音ゲーで最重要。

ノーツ進行を `setInterval()` のカウントだけに依存させない。

Web Audio API / AudioContext を基準とした高精度の時間管理を行う。

考え方:

```text
audioStartTime
+
AudioContext.currentTime
=
現在の楽曲時刻
```

描画は `requestAnimationFrame` を利用し、各フレームで現在時刻からノーツの表示状態を算出する。

フレーム落ちが発生しても譜面時刻そのものがズレない設計にする。

---

# 22. ノーツサークル

ノーツの基準時刻を `T` とする。

例:

```text
T - 1000ms: ノーツ出現
T - 1000〜0ms: 外周サークル縮小
T: サークルとノーツが一致
T + 250ms: 未入力ならMISS
```

設定値:

```ts
NOTE_APPROACH_MS = 1000;
PERFECT_WINDOW_MS = 80;
GREAT_WINDOW_MS = 150;
GOOD_WINDOW_MS = 250;
```

これらは一箇所で管理する。

---

# 23. カメラ座標と画面座標

カメラはユーザー向けに水平反転表示する。

MediaPipe座標とUI座標の左右差に注意する。

座標変換処理を共通関数にまとめ、各コンポーネントで個別実装しない。

テスト対象とする。

---

# 24. レスポンシブ

主対象:

1. PC
2. ノートPC

スマートフォンはMVPの主要対象外。

理由:

- カメラ前で身体を動かすゲーム
- 画面サイズが必要
- ランドスケープ前提

小画面では「PC推奨」を表示してもよい。

---

# 25. パフォーマンス

目標:

- ゲーム描画: 60fpsを目標
- MediaPipe推論とReact再描画を分離
- ランドマーク値を毎フレームReact stateへ大量格納しない
- Canvas または軽量なDOM描画を利用
- カメラ処理中の不要なオブジェクト生成を避ける

ゲーム中はUI更新範囲を限定する。

---

# 26. エラー処理

以下を個別に扱う。

- カメラ拒否
- カメラがない
- MediaPipe初期化失敗
- 音源ロード失敗
- 音声自動再生制限
- Supabase取得失敗
- スコア送信失敗
- ネットワーク切断

ゲーム自体はランキング通信が失敗してもローカルでRESULTまでは表示する。

スコア送信失敗時は明確に表示する。

---

# 27. プライバシー

カメラ映像はブラウザ内で処理する。

MVPでは以下を禁止する。

- カメラ映像のサーバー送信
- 録画
- 顔画像保存
- Supabaseへの画像保存

ユーザーにCamera Check画面で、

「カメラ映像は手の認識に使用し、サーバーへ送信しません」

と分かるように表示する。

---

# 28. テスト

## 28.1 Unit Test

最低限:

- score計算
- accuracy計算
- timing判定
- chart validation
- coordinate変換
- gesture state transition
- clap cooldown
- ranking query helper

## 28.2 Integration Test

- play session発行
- score submit
- 不正noteId拒否
- 不正chartVersion拒否
- 重複session使用拒否
- session期限切れ拒否
- 正常スコア登録
- ランキング取得

## 28.3 E2E

最低限:

```text
Title
→ START
→ Song Select
→ Difficulty
→ Camera Check
→ Game
→ Result
→ Ranking
```

カメラ部分はテスト用モックを利用可能にする。

---

# 29. 譜面固定テスト

特に重要。

同一の:

```text
songId
difficulty
chartVersion
```

から読み込まれる譜面JSONのハッシュが、ビルドごとに意図せず変化していないことを検出できるようにする。

譜面変更時は必ず `chartVersion` を上げる。

例:

```text
normal v1
↓ 譜面修正
normal v2
```

既存ランキングはv1として保持するか、新ランキングへ切り替える。

**譜面を変更したのにchartVersionを維持することは禁止。**

---

# 30. ランキング公平性

以下が変化した場合はランキング条件を分離する。

- 譜面
- 判定幅
- スコア計算
- 曲の同期オフセット

そのため以下をバージョン管理する。

```text
chartVersion
scoreVersion
```

必要であれば将来的に:

```text
judgeVersion
```

も追加可能。

---

# 31. Codex実装ルール

Codexは以下を守る。

1. 本仕様書を実装の唯一の基準とする
2. `資料/` の実ファイルを確認してから素材パスを決める
3. 不明点があってもアーキテクチャを無断で大幅変更しない
4. ゲームロジックとUIを分離する
5. MediaPipe処理とゲームエンジンを分離する
6. スコア計算はクライアント/サーバーで共有可能な純粋関数にする
7. TypeScriptの`any`を原則使わない
8. エラーを握りつぶさない
9. console.logを大量に残さない
10. 秘密情報をハードコードしない
11. Supabase Secretをクライアントへ露出しない
12. 曲追加のためにソースロジック変更を要求しない
13. 譜面は決定論的にする
14. 同じ曲・難易度でランダムノーツを生成しない
15. テスト可能な構成を優先する
16. UIは過剰なAI感を避ける
17. 実装前に既存ファイルを確認し、不要な全書き換えを避ける
18. 変更ごとにlint/typecheck/testを実行する

---

# 32. 実装優先順位

## Phase 1: Skeleton

- Next.js
- Title
- Menu
- Song Select
- Game画面
- Result
- 仮データ

## Phase 2: MediaPipe

- Camera
- Hand Landmarker
- FIST
- GUN
- OPEN
- CLAP
- Camera Check

## Phase 3: Rhythm Engine

- audio timing
- chart loader
- notes
- shrinking circle
- position判定
- timing判定
- score
- combo
- result

## Phase 4: Assets / UI

- 資料音源利用
- SFX
- ジャケット
- ノーツ画像
- 近未来UI
- アニメーション
- エフェクト

## Phase 5: Supabase

- Auth
- Profile
- play session
- score validation
- ranking
- RLS

## Phase 6: Deploy

- GitHub
- Vercel
- env
- Preview
- Production

## Phase 7: Polish

- performance
- calibration
- error handling
- responsive
- accessibility
- E2E
- security review

---

# 33. MVP完成条件

以下をすべて満たせばMVP完成とする。

- [ ] Titleからゲーム開始可能
- [ ] 1曲以上選択可能
- [ ] EASY/NORMAL/HARDが存在
- [ ] 各難易度の譜面が固定されている
- [ ] 同一曲・同一難易度なら毎回同じノーツ
- [ ] カメラが全画面背景になる
- [ ] MediaPipeで最大2手認識
- [ ] FIST認識可能
- [ ] GUN認識可能
- [ ] OPEN認識可能
- [ ] CLAP認識可能
- [ ] ノーツがカメラ上の指定位置に表示
- [ ] ノーツ周囲に縮小サークル表示
- [ ] 正しい位置+形+タイミングで判定
- [ ] PERFECT/GREAT/GOOD/MISS表示
- [ ] ヒット時に対応SFX再生
- [ ] SCORE表示
- [ ] COMBO表示
- [ ] RESULT表示
- [ ] Supabaseランキング登録
- [ ] 楽曲選択画面に1位表示
- [ ] 全ランキング画面表示
- [ ] スコアをブラウザから直接自由にINSERTできない
- [ ] RLS有効
- [ ] SecretをGitHubへ含めない
- [ ] GitHub→Vercel自動デプロイ
- [ ] 新曲追加がデータ追加中心で完結
- [ ] lint成功
- [ ] typecheck成功
- [ ] test成功
- [ ] Production build成功

---

# 34. 完成後の拡張候補

MVPには必須ではない。

- Expert
- 両手同時ノーツ
- Long Note
- Hold Gesture
- Daily Challenge
- Friend Ranking
- Replay
- Player Level
- Achievement
- 曲プレビュー
- Full Combo
- All Perfect
- 譜面エディター
- 自動BPM解析
- AIによる譜面候補生成
- オンラインイベント

AIで譜面候補を生成する場合も、本番で利用する譜面は人間が確認後JSONとして固定し、ゲーム中には生成しない。

---

# 35. 最終的な体験

ユーザー:

```text
サイトへアクセス
↓
START
↓
曲を選ぶ
↓
難易度を選ぶ
↓
ランキング1位を見る
↓
PLAY
↓
Camera Check
↓
楽曲開始
↓
カメラ映像上にノーツが出現
↓
サークルが縮む
↓
指定位置へ手を動かす
↓
✊ / 👉 / ✋ / 👏
↓
PERFECT!
↓
音が鳴る
↓
コンボ
↓
RESULT
↓
ランキング登録
↓
もう一度挑戦
```

Hand Beatの中心は、MediaPipeそのものではない。

**「自分の手を音楽に合わせて動かし、画面の中へ直接触れているような感覚で遊べること」**

を最優先に実装すること。
